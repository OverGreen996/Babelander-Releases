# 雲端額度診斷（1.4.3）

在「功能引擎與雲端設定 → 雲端額度診斷（詳細資訊）」查看。本次執行保留最近 200 個翻譯區塊，關閉程式即清除；不用 API、不初始化未使用的 Provider。沒有原文、譯文、API Key 或原始錯誤訊息，只有字數、功能、時間、雜湊和狀態。原本的用量資料庫繼續保留有界的長期彙總。

每筆顯示辨識／輸入與正規化字數、本機規則解決字數、候選 Payload 字數、實際請求次數、手動重試、快取、合併、增加的 attempted／confirmed 字數與送出後的本期累計。卡片摘要獨立累計所有已處理區塊，保留最近 50 張已完成卡片；不受最近 200 筆請求淘汰影響。快取可能在準備 Payload 前就命中，此時 Payload 為 0；命中傳輸快取則可看到候選字數，但實際請求與增加用量仍為 0。

計數使用三家 HTTP Adapter 最終送出的 source text，於 HTTP POST 前通過原子配額檢查並預扣。DNS／TLS 連線失敗而未送出不扣字；HTTP 送出失敗或回應逾時保留嘗試字數，不猜測官方是否計費。只有收到有效成功回應才增加 confirmed。不是官方帳單，也不計入本機 prompt、辭典或沒有傳送的參考上下文。

仍然沒有自動重試。失敗後使用者再次傳送相同 Payload，會記錄重試關係與獨立扣字；詳細資訊可加總仍保留的重試鏈。程式重開或超出 200 筆的紀錄無法回溯，舊版本歷史 2,000 字也無法事後還原。

同一服務、語言方向、Payload、設定與翻譯語境共用進行中的工作及至多 128 筆成功回應快取；語境包括範圍、詞庫版本、術語對應及參考內容。同語境可跨功能共用，個別功能仍使用既有驗證。取消其中一個呼叫不影響仍等待的呼叫，取消全部則丟棄結果。只用精確匹配，不使用模糊去重；+50 與 +500 不會被當成相同內容。設定變更會清除傳輸快取，驗證失敗結果不保留於快取。

裝備／任務沿用先解析、再詞庫／文法、最後才送未知段落的流程。新增保守的完整句拆分：同段若有已知規則句，只翻譯剩餘獨立敘述，再用原本驗證核心核對整段。代名詞、銜接語、引號、括號與條件不明的段落不強行拆開；字幕不套用卡片拆句。聊天室來訊仍固定本機。

中翻英只傳本次使用者輸入經術語／ID／數字保護後的文字。保護標記本身也真的被傳送，可能令 Payload 比短原文更長，因此照實計數，沒有隱藏這項成本。

## 離線量測

使用真實 Google／Azure／DeepL Adapter 和用量資料庫，以模擬 HTTPS 回應測試，未使用真實 Key 或產生 API 費用。三家結果一致；以下為指定範例，不代表所有遊戲的節省率。

| 範例 | 原始字數 | 優化前 Payload | 優化後 Payload |
|---|---:|---:|---:|
| 5 區塊裝備卡 | 94 | 29 | 29 |
| UI／標題／NPC／目標／故事任務卡 | 118 | 69 | 69 |
| 已知裝備效果句＋未知敘述 | 59 | 59 | 29 |
| 已知任務目標句＋未知敘述 | 48 | 48 | 29 |
| 完整字幕 | 29 | 29 | 29 |
| 中翻英「等 5 分鐘」 | 6 | 14 | 14 |

整組由 248 降至 199 個嘗試字（約 19.8%），請求皆為 6 次。十次相同裝備卡只送一次未知敘述；同時相同請求合併一次；送出後逾時再手動重試，兩次都計費風險預扣。Azure 1,900,000 字硬上限維持原值。可用 `python tools/audit_cloud_payload.py` 重現離線量測，測試環境需安裝開發依賴。

## 完成確認

Google／Azure／DeepL 均在有 Key 時按個別設定「完成」必定顯示使用確認，連線測試為選用；取消保留設定視窗。總設定頁也會檢查已存 Key 但未同意的服務。沒有 Key 可以略過；同意不會自動切換引擎、不會自行發送測試、不會解除官方或本機額度鎖定。

## 1.4.3 單張卡片與長期統計

「整張卡片成本」包含 OCR Total、正規化、本機規則、數值、插槽、候選 Payload、attempted、confirmed、請求、手動重試、快取、合併與失敗區塊。低 OCR 信心區塊標為失敗，不假裝節省成功。取消／中斷標記為 incomplete，只列已完成部分。整張卡的節省率為 `1 - attempted / OCR Total`；標記膨脹或重試可能產生負值。候選 Payload 包含命中快取或合併的候選，不等於實際扣字；外層快取節省採來源長度估計，不宣稱精確的官方帳務節省。

「用量分析」顯示今日／本機本期各服務、功能的 attempted、confirmed、占比、請求、重試、快取、合併、本機規則。每日資料以該服務明確的時區分桶；送出後跨日收到成功回應仍歸屬原送出日期。只有數字，沒有翻譯文字、Key 或截圖。升級前沒有功能分項的舊用量不猜測回填。

每日聚合保留 400 天，較舊的併入月桶，約 36 個月以前再合併為一筆 provider × feature 長期總數。最近 7 天（含當日未完結日）平均僅為估計，首次有資料至今的未使用日期也納入分母。安全額度帳本獨立運作，診斷完成時寫入的聚合若遇強制結束／磁碟故障可能漏記；不能拿分析頁代替官方帳單或安全帳本。

## 各服務時間規則

- Google：維持 America/Los_Angeles 日／月界線，450,000 本機月安全線。
- Azure：產品明確選定 Babelander Calendar Month = America/Los_Angeles；維持 1,900,000 安全線及既有資料相容，不宣稱等同 Azure 帳單時區。
- DeepL：獨立 UTC billing_day，小月仍取月底。Developer／年繳／未設定週期不憑空月重置。舊版 Pacific 重置日期第一次遷移會保留其實際時間，不會在較早的 UTC 午夜先補額度。

## DeepL 官方用量基準

設定頁保留「讀取官方用量」，另有「同步官方用量基準（手動）」。只有按下才 GET /v2/usage，不隨翻譯輪詢。同步儲存官方 used／limit／period／synced_at，以及必要的本機累計錨點和安全 floor；沒有翻譯內容。

`safe_usage = max(本機本期 attempted, floor + 同步後本機 attempted 增量)`。

查詢前捕捉本機錨點，查詢期間的新嘗試也算進去，可能保守重算但不漏掉這些請求。相同週期的較低官方數字不降低先前的安全估算；attempted 不被同步改寫。實際送出以 safe_usage 與「設定上限／官方上限的較小值」原子檢查；0 額度會鎖定。同步不清除官方錯誤、不改永久引擎或暫用選擇。

若回傳有效官方起訖時間，基準跨本機月界線保留；到官方 end_time 即停止新送出並要求手動同步，不擅自假設下期用量為零。只有已驗證的下一官方週期可退休上期基準，當期本機 attempted 仍保留。官方缺少日期時，基準隨使用者已設定的本機 UTC 週期；沒有本機週期則不自動重置。換 Key 不會清空安全計數。

官方用量可能延遲幾分鐘；API Free／Pro Classic 可能只回傳 count／limit，不能自行捏造日期。參考：[DeepL 官方 usage 文件](https://developers.deepl.com/api-reference/usage-and-quota/check-usage-and-limits)。其他裝置在同步後的用量仍未知，安全估算不保證整個帳戶不超額。

## Metrics 隱私

ResultStorage 的 metrics 預設只存 allowlist：字數、SHA-256、固定分類、座標、信心、時間及布林值；source／raw_source／target、自由格式 warning／note／failure、巢狀 OCR 文字皆不落盤。既有 metrics 首次開啟會做一次性去全文轉換。原有翻譯快取與歷史功能、容量限制不變，本變更不宣稱把所有翻譯歷史變成匿名或進行磁碟取證等級抹除。

「文字除錯（選用）」的 Debug Translation Trace 預設 OFF。主動開啟才保留 source／raw_source／target，最多 50 筆且總計 256 KiB；只在 RAM，關閉開關或關閉程式清除，不保存設定為 ON。保留期間可手動重新整理查看。

## 後續真實環境驗證

尚待實際遊戲數天觀察：每日 API、功能比例、命中／拆句節省、重試／錯誤、RAM／VRAM、冷啟動、暖機及資料庫大小。本版不會替使用者開啟付費測試或背景輪詢，也不把模擬 HTTPS 測試說成真實使用天數。整理包體及離線時間／大小量測後，只對可重現的問題做小範圍清理，不大改 Provider、Scheduler、Model Lifecycle 或驗證核心。
